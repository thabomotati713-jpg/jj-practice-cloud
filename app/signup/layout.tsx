import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register Your Practice | J&J Practice Cloud",
  description:
    "Register a South African healthcare practice for J&J Practice Cloud. No card required; guided onboarding follows registration.",
};

export default function SignupLayout({ children }: { children: React.ReactNode }) {
  return children;
}
